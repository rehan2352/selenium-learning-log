# ✅ Conditional Methods in Selenium WebDriver

This Java program demonstrates the use of **conditional methods** in Selenium WebDriver such as:

- `isDisplayed()` – checks if an element is visible on the web page.
- `isEnabled()` – checks if an element is interactable (not disabled).
- `isSelected()` – checks if a checkbox or radio button is selected.

These methods are essential in test automation for decision-making and element validation.

---

## 🚀 Technologies Used

- Java
- Selenium WebDriver
- ChromeDriver

---

## 🧪 Script Overview

The program opens the [Automation Testing Demo Site](https://demo.automationtesting.in/Register.html) and performs:

1. Launch browser and navigate to the registration page.
2. Maximize browser window.
3. Check if the header is displayed using `isDisplayed()`.
4. Verify if the First Name field is enabled using `isEnabled()`.
5. Check if the "Male" radio button is enabled.
6. Check if the "Male" radio button is selected using `isSelected()`.

---

## 📂 Folder Structure

